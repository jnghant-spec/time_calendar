import 'package:flutter/material.dart';
import 'package:time_calendar/models/list_event.dart';

/// 与 [ListPage] 中 `showEventEditSheet` 调用约定一致；完整表单可后续接入。
Future<ListEvent?> showEventEditSheet(
  BuildContext context, {
  required bool isVip,
}) async {
  return null;
}
